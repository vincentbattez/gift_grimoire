/** Math.random() wrapper for visual/animation purposes only — not for security. */
export function randomVisual(): number {
  // eslint-disable-next-line sonarjs/pseudo-random
  return Math.random();
}
